# 演示视频剧本（2 分钟）

> 主线：从零实现的类 Claude Code 编程智能体，核心是**人机协作的审查闭环**——
> 计划审查 → 工具审批 → 改动回滚 → 跨会话记忆，外加声明式技能与上下文管理。
>
> 全片用一个**完整的编程任务**贯穿：实现一个 markdown 表格 → CSV 转换小工具，
> 含源码、单元测试、运行验证。以下分镜基于真实 API 验证过的输出撰写。

---

## 录制前准备

```bash
# 1. 建一个全新演示目录 —— ⚠️ 必须在任何 git 仓库之外！
#    否则 agent 判定"已有仓库"进入只读监视模式，镜头 7 的 git reset 无法演示。
#    建议用 HOME 下目录（~ 不在任何仓库内），不要在 ~/nju-se-coding-agent 里建。
mkdir -p ~/demo-agent

# 2. code-review 技能是 agent 内置兜底，无需复制（0.4.0 起自动发现自带技能）
#    若想演示"项目自定义技能"，才在演示目录里放 .agents/skills/（注意拼写带 s）

# 3. 录制环境
#    - macOS: Cmd+Shift+5 区域录制，或 QuickTime 新建屏幕录制
#    - 终端字号调大、窗口干净无杂项、深色主题对比清晰
#    - 每一镜先排练 1~2 遍（模型输出有随机性），确认节奏后正式录
```

---

## 分镜表

### 镜头 1｜开场：零第三方依赖（0:00–0:12）

- **画面**：项目根目录，列出核心文件
- **命令**
  ```bash
  cd ~/nju-se-coding-agent
  ls *.py tools/
  ```
- **口播**：*"这是我用 Python 标准库从零实现的编程智能体。HTTP 层、SSE 流式解析、token 估算、Markdown 渲染，全部自写——零第三方依赖。"*

### 镜头 2｜启动 REPL + 技能发现（0:12–0:22）

- **画面**：进入交互模式，看到「git 模式：自动 checkpoint」与技能清单（code-review 为内置）；输入 `/skills`
- **命令**
  ```bash
  cd ~/demo-agent          # 仓库外目录 → checkpoint 模式
  python ~/nju-se-coding-agent/main.py
  /skills
  ```
- **口播**：*"进入交互式 REPL，工作目录自动初始化成 git 仓库。它还支持声明式技能——agent 自带 code-review，项目里放自己的 SKILL.md 也能扩展，无需改代码。"*

### 镜头 3｜详细任务 + 计划先行（0:22–0:40）

- **画面**：开启计划模式，下达详细任务；模型给出分步计划；人工确认
- **命令**
  ```
  /plan
  /approve
  写一个小工具：从文件读取 markdown 表格并转换成 CSV 输出到 stdout，
  要处理表头、分隔行、以及含逗号或双引号的单元格，
  并用 unittest 写单元测试覆盖这些情况，最后全部跑通验证。
  y        ← 确认计划
  ```
- **口播**：*"执行前 agent 先给出完整的分步计划，必·须我确认才动手。计划不满意还能多轮修改、重拟——这是审查链的第一道门。"*

### 镜头 4｜写源码 + 审批 + 拒绝留言 → 模型自愈（0:40–1:00）

- **画面**：模型写源码，弹彩色 diff；在审批框按 `m` 留言改名 → 模型说"明白"并重写 → 逐次按 `y` 放行
- **命令**
  ```
  m
  文件名用 md2csv.py
  y
  ```
- **口播**：*"每个文件改动和命令都会先弹 diff 让我审批。我拒绝并留言，模型把我的意见回喂给自己、调整方案重来——这就是 Codex 式的人机协作审查。"*
- **提示**：若模型一开始就用了好名字，可改拒绝点（如"测试文件名改成 tests/test_md2csv.py"或"不要创建 demo.md"）。

### 镜头 5｜写测试 + 跑通（1:00–1:15）

- **画面**：模型写 `test_md2csv.py`（approve）→ 运行 `python3 -m unittest test_md2csv.py -v`（approve）→ 全部通过
- **口播**：*"它自己写单元测试并运行验证。实测 16 个用例全部通过，覆盖了表头、分隔行、含逗号引号的单元格等边界。"*

### 镜头 6｜调用技能审查代码（1:15–1:32）

- **画面**：模型调用 `invoke_skill` 加载 code-review → 读文件 → 输出 🔴🟡🟢 分级、带行号的审查意见与结论
- **命令**
  ```
  用 code-review 技能审查一下刚写的代码
  ```
- **口播**：*"它主动调用技能审查自己的代码，按严重程度分级、标注行号、给出结论和下一步——这套行为完全来自 SKILL.md。"*

### 镜头 7｜git checkpoint + 一键回滚（1:32–1:48）

- **画面**：`/review git` 查看最近 checkpoint 的 diff → `/review git reset` 回滚，文件消失
- **命令**
  ```
  /review git
  /review git reset
  ```
- **口播**：*"每轮任务自动打 git checkpoint，命令的副作用——比如装的依赖、构建产物——也能被覆盖；一个命令回滚到上一检查点。"*

### 镜头 8｜跨会话记忆 + 收尾（1:48–2:00）

- **画面**：让 agent 记住约定 → 重启进程 → 问"项目用什么测试框架？我有什么偏好？" → 正确召回 → `/usage` 看消耗
- **命令**
  ```
  记住：本项目用 pytest 测试；用户偏好中文注释
  # Ctrl-D 退出，重新启动
  python ~/nju-se-coding-agent/main.py
  我们项目用什么测试框架？我有什么偏好？
  /usage
  ```
- **口播**：*"agent 有跨会话长期记忆，重启后仍记得项目约定和你的偏好；上下文有 token 预算、会压缩、会校准。从计划、审批、回滚到记忆——一条完整、可插拔、可观测、可恢复的工程闭环。"*

---

## 排练清单（每镜录前确认）

- [ ] 镜头 1：`ls` 能清晰看到核心模块名，口播与画面同步
- [ ] 镜头 2：启动后出现「技能：code-review」，`/skills` 正常列出
- [ ] 镜头 3：计划条理清晰；`y` 确认后进入执行
- [ ] 镜头 4：`m` + 留言后，模型**确实改了文件名**再写（若没改可重来）
- [ ] 镜头 5：测试真的通过（`OK` / `Ran N tests`）
- [ ] 镜头 6：审查输出含 严重/一般/建议 分级与行号
- [ ] 镜头 7：`/review git reset` 后文件消失、目录回到 checkpoint 前
- [ ] 镜头 8：重启后 agent 能说出"pytest"与"中文注释"

## 录制注意事项

1. **git 模式**：同一目录二次启动会进入「只读监视（已有仓库）」模式，自动提交/回滚被禁用——所有 git 演示放在镜头 7 的**同一个会话**内完成；镜头 8 重启后的「只读监视」提示可顺带讲成安全特性。
2. **批量审批**：模型可能一次调多个命令，会连续弹多个审批框，逐个按 `y`。
3. **随机性**：模型行为非完全确定，务必排练；镜头 4/6 若行为不对，重新进一轮即可。
4. **总时长**：全片 ≈ 115~120s，收尾别拖。
